/// <reference path="../types/jasmine.d.ts" />

import Stack from "../Stack";

export default describe('Stack', () => {
  it('foo', () => {
    expect(Stack.foo).toBe(9);
  });

  it('bar', () => {
    expect(1).toBe(1);
  });
});
